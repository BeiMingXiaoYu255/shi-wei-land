<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 配置环境：可以配置多个环境，default指定默认环境 -->
    <environments default="development">
        <environment id="development">
            <!-- 事务管理器：JDBC（使用JDBC的事务管理）、MANAGED（交由容器管理） -->
            <transactionManager type="JDBC"/>
            <!-- 数据源：POOLED（连接池）、UNPOOLED（非连接池）、JNDI（容器数据源） -->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/mybatis_demo?useSSL=false&amp;serverTimezone=UTC&amp;characterEncoding=utf8"/>
                <property name="username" value="root"/> <!-- 替换为你的MySQL用户名 -->
                <property name="password" value="2550505"/> <!-- 替换为你的MySQL密码 -->
            </dataSource>
        </environment>
    </environments>

    <!-- 配置映射器：指定MyBatis的Mapper文件或接口位置 -->
    <mappers>
        <!-- 指定Mapper XML文件的位置 -->
        <package name="cn.cjlu.shiweiland.mapper"/>
    </mappers>
</configuration>